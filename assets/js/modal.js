document.addEventListener('DOMContentLoaded', function () {
    var myModal = new bootstrap.Modal(document.getElementById('condiciones'), {
      keyboard: false
    });
    //  la siguiente línea permite que el modal se muestre automáticamente
    // myModal.show();
  });